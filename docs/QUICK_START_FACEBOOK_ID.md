# 🚀 بدء سريع - البحث بـ Facebook ID

## ✅ الخطوات المطلوبة

### 1️⃣ إضافة الـ Index على قاعدة البيانات

**مهم جداً**: لضمان البحث السريع حتى مع ملايين الصفوف!

```bash
# من Terminal
mysql -u bot_user -p < docs/mysql_indexes_facebook_id.sql
```

سيضيف Index على `facebook_id` في كلا القاعدتين (VIP و Regular).

---

### 2️⃣ التأكد من تشغيل السيرفر

السيرفر يجب أن يكون شغال:

```bash
npm run dev
```

أو للبرودكشن:

```bash
npm run start:prod
```

---

### 3️⃣ الاستخدام في Telegram

ببساطة، أرسل Facebook ID للبوت:

```
100012345678
```

البوت هيكتشف تلقائياً إنه Facebook ID ويبحث عنه! 🎯

---

## 🧪 اختبار الـ Tool مباشرة (اختياري)

للمطورين، يمكنك اختبار الـ tool مباشرة:

```bash
curl -X POST http://localhost:5000/api/agents/Telegram%20Phone%20Lookup%20Bot/generate \
  -H "Content-Type: application/json" \
  -d '{
    "messages": [{"role": "user", "content": "100012345678"}],
    "runtimeContext": {"telegramUserId": 123456789}
  }'
```

**ملاحظة**: غير `123456789` برقم telegram user ID حقيقي موجود في قاعدة البيانات.

---

## 📊 أنواع البحث المتاحة الآن

البوت الآن يدعم نوعين من البحث:

| نوع البحث | مثال | الأداة المستخدمة |
|-----------|------|-------------------|
| **رقم الهاتف** | `+201234567890` أو `01234567890` | `phone-lookup` |
| **Facebook ID** | `100012345678` | `facebook-id-lookup` |

البوت يكتشف النوع تلقائياً! 🤖

---

## ⚡ الأداء المتوقع

مع الـ Index المضاف:

- **بدون Index**: 5000-10000ms على مليون صف 🐌
- **مع Index**: 2-5ms فقط! ⚡

**تحسن 2000x في الأداء!**

---

## ❓ استكشاف الأخطاء

### المشكلة: البحث بطيء جداً
**الحل**: تأكد من إضافة الـ Index (خطوة 1)

```bash
mysql -u bot_user -p < docs/mysql_indexes_facebook_id.sql
```

### المشكلة: البوت لا يستجيب
**الحل**: تأكد من تشغيل السيرفر

```bash
# تحقق من الـ workflow
# شوف إذا "Start application" شغال
```

### المشكلة: "ليس لديك اشتراك نشط"
**الحل**: أضف المستخدم لقاعدة البيانات

```sql
USE telegram_bot_vip;
INSERT INTO user_subscriptions (telegram_user_id, username, subscription_type, is_active)
VALUES (123456789, 'username', 'vip', TRUE);
```

---

## 🎉 انتهى!

الآن البوت جاهز للبحث بـ Facebook ID! 

للمزيد من التفاصيل، راجع: [FACEBOOK_ID_SEARCH.md](FACEBOOK_ID_SEARCH.md)
